# handoff

## 현재 상태

- 반포자이 보유·양도 최적화 시뮬레이터의 단일 HTML SPA는 `src/banpo_xi_tax_simulator.html`에 있습니다.
- Google Apps Script 웹앱과 GitHub Pages 배포 구성이 준비돼 있습니다.
- GitHub Pages는 `gh-pages` 브랜치에서 제공되며, 운영 주소는 <https://tax.dbserver.dedyn.io>입니다.
- 소유 형태는 부부 공동명의 50:50(기본값)과 단독명의 100% 중 선택할 수 있습니다.
- 새 세션의 코드·배포 경로 참조 인덱스는 `ref.md`입니다. 이 문서는 현재 상태와 다음 작업 우선순위만 관리합니다.
- 작업 후 문서 갱신 체크리스트는 `update.md`입니다. 새 문서·세법·배포 경로 변경 시 `ref.md`와 함께 확인합니다.

## 완료 작업

- `AGENTS.md`의 세법 계산식에 맞춰 종부세 과세기준일 전·후를 포함한 9개 매도 시나리오, 슬라이더 기반 재계산, Chart.js 비용·순이익 차트를 구현했습니다.
- 차트 재렌더링 전 기존 인스턴스를 `destroy()`하도록 구성했습니다.
- Apps Script HTML Service 진입점(`src/Code.gs`) 및 매니페스트(`src/appsscript.json`)를 구성해 웹앱을 배포했습니다.
- GitHub `donbkim/tax` 저장소의 `main` 브랜치에 원본을, `gh-pages` 브랜치에 GitHub Pages용 정적 산출물을 배포했습니다.
- `tax.dbserver.dedyn.io`의 GitHub Pages 연결과 HTTPS 응답을 확인했습니다.
- 소유 형태 토글을 추가했습니다. 공동명의는 인당 과세대상 양도차익·공제 한도·기본공제·누진세를 2인에게 적용하며, 단독명의는 전체 양도차익에 1회 적용합니다.
- 명의 전환 시 헤더, 취득가액 라벨, 비교표 주석, 공동명의 해설, 요약·아비트리지 금액을 함께 갱신하도록 했습니다.
- 2026 세제개편 수정안을 반영했습니다. 비거주 단독명의 기본공제는 12억 원으로 유지하고, 비거주 공동명의는 인당 6억 원(합산 12억 원), 세부담 상한은 150%로 계산합니다.
- 2026년 비거주 매도와 2027~2029년 종부세 과세기준일(6월 1일) 전·후를 구분한 9개 시나리오를 추가했습니다. 비교표에 2028년 인당 20억 원 및 2029년 이후 인당 10억 원 양도소득공제 한도의 적용 여부를 표시합니다.
- 고정 매도가 기준 막대 뒤에 2026년 기준 연 5% 집값 복리 상승을 반영한 총비용·상대 순이익을 회색 음영으로 표시합니다. 상승 매도가에 따른 양도세도 함께 재계산합니다.
- 종부세는 누적 보유세가 아닌 매도 해당 연도의 세액으로 계산합니다. 따라서 6월 1일 전 매도 시 0원, 후 매도 시 해당 연도의 추정 세액만 총비용·비교표·비용 차트에 반영합니다.
- 2026-09-03에 Apps Script 공개 배포를 `@7`(종부세 해당 연도 세액 반영)으로 갱신했습니다. GitHub Pages용 커밋은 생성했지만, 작업 환경의 GitHub HTTPS 인증 부재로 `gh-pages` 원격 푸시는 보류됐습니다.
- 가격상승 음영 변경은 Apps Script 공개 배포 `@6` 및 GitHub Pages에 반영했고, 운영 도메인 응답이 최신 Pages HTML과 일치하는 것을 확인했습니다.

## 검증 상태

- 로컬 브라우저에서 초기 화면, 탭 전환, 매도가 슬라이더 변경과 초기화 동작을 확인했습니다.
- 기본값에서 시나리오 4가 `최적`으로 강조되는 것을 확인했습니다.
- `https://tax.dbserver.dedyn.io/`에서 HTTP 200 응답을 확인했습니다.
- 공동/단독명의 각각에서 양도세·절세액·수익률·시나리오 4 강조가 갱신되고, 기본값 초기화 시 공동명의로 복귀하는 것을 로컬 브라우저에서 확인했습니다.
- 로컬 브라우저에서 공동명의 기본값의 해당 연도 종부세(2028년 비거주·부과 후 1,797만 원, 거주·부과 후 1,102만 원), 6월 1일 전 매도의 0원 표시, 시나리오 4 강조 및 매도가 슬라이더 재계산을 확인했습니다.
- Apps Script 공개 배포 `@7`이 생성됐습니다. GitHub Pages는 HTTP 200이나 아직 기존 `누적 종부세` 문구를 반환하므로, `gh-pages` 푸시 후 재확인이 필요합니다.
- 로컬 브라우저에서 연 5% 집값 상승 음영이 두 차트의 고정 매도가 막대 뒤에 표시되고, 2029년 시나리오의 상승 매도가와 양도세가 재계산되는 것을 확인했습니다.

## Recommended next step

종부세 추정모형의 공시가격·세율·재산세 공제 전제를 실제 고지 자료로 고도화합니다.

1. 반포자이 84㎡의 연도별 공시가격과 재산세를 확보합니다.
2. 공동·단독명의별 누진세율과 세액공제를 반영한 종부세 산식으로 현재의 비례 추정 방식을 교체합니다.
3. 산식 고도화 후 공동/단독명의 각각에서 양도소득세·종부세·총비용·차트를 재검증하고 두 배포본을 갱신합니다.

## 주의사항

- `AGENTS.md`의 단일 HTML 원칙을 유지합니다. CSS, JavaScript, JSON을 별도 파일로 분리하지 않습니다.
- `src/banpo_xi_tax_simulator.html`의 차트 캔버스는 계속 `.chart-container`로 감싸야 합니다.
- `.env`와 `.clasp.json`은 인증·연결 정보를 포함하므로 출력하거나 커밋하지 않습니다.
- `webapp.md`는 초기 참고 자료이며 현재 Git 추적 대상이 아닙니다. 사용자 원본으로 취급합니다.
